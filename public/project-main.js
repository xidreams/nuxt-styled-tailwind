function alertMsg(msg) {
    alert('msg='+msg);
}

document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('myButton');

    //button.addEventListener('click', () => {
        //alertMsg('Button was clicked!'); // Call your function here
    //});
});